firstly i clean database code by create a DB>index.ts 
1. in DB>index.ts here only represent for database code
then i clean server and app code
2. server.ts only for server running code 
3. app.ts for all api code etc..

here i create a modules folder > user
i. then i create a router file for user.route.ts and Handles all user-related routes there
ii. user.controller.ts for Handles request and response logic.
iii. user.service.ts for Contains database queries and business logic.
iv. user.interface.ts for Defines TypeScript interfaces and types.